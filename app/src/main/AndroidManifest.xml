<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.googlemaps">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".help"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".sudha"
            android:label="@string/title_activity_sudha" />
        <activity
            android:name=".ratna"
            android:label="@string/title_activity_ratna" />
        <activity
            android:name=".para"
            android:label="@string/title_activity_para" />
        <activity
            android:name=".child"
            android:label="@string/title_activity_child" />
        <activity
            android:name=".ortho"
            android:label="@string/title_activity_ortho" />
        <activity
            android:name=".cas"
            android:label="@string/title_activity_cas" />
        <activity android:name=".dpage" />
        <activity
            android:name=".disease"
            android:label="@string/title_activity_disease" />
        <activity
            android:name=".organ"
            android:label="@string/title_activity_organ" />
        <activity
            android:name=".blood"
            android:label="@string/title_activity_blood" />
        <activity
            android:name=".ambulance"
            android:label="@string/title_activity_ambulance" />
        <activity
            android:name=".pharmacy"
            android:label="@string/title_activity_pharmacy" />
        <activity
            android:name=".mumbai"
            android:label="@string/title_activity_mumbai" />
        <activity
            android:name=".pune"
            android:label="@string/title_activity_pune" />
        <activity android:name=".main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".bhala1" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>